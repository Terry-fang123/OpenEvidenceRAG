# ≤3步检索—核验 Agent + Trace 说明

## 一、概念说明

“≤3步检索—核验 Agent + Trace”是指：构建一个能够在最多三个主要步骤内，自动完成证据检索、引用核验以及回答或拒答决策的智能体，并保存每一步可审计的执行记录。

- **≤3步**：一次任务最多执行三个主要行动步骤，避免无限搜索和流程失控。
- **检索—核验 Agent**：系统不只按固定流程检索，而是能够根据检索结果决定是否继续核验、补充检索、正常回答或结构化拒答。
- **Trace**：记录每一步使用的动作、输入、结果摘要和最终决策，方便复核与展示。

这里的 Trace 是系统外部行为记录，不是大模型隐藏的完整思维过程。

## 二、推荐的三步流程

### 第1步：混合检索

系统根据用户问题生成检索词，同时调用：

- 本地医学证据库；
- PubMed在线文献；
- BM25关键词检索；
- 向量语义检索；
- 可选Rerank精排。

示例问题：

> 贝派地酸能降低多少LDL-C？

Trace示例：

```json
{
  "step": 1,
  "action": "search",
  "query": "bempedoic acid LDL-C reduction randomized trial meta-analysis",
  "result": {
    "candidate_count": 15,
    "selected_count": 5,
    "selected_pmids": ["42236342", "41755916"]
  }
}
```

### 第2步：引用核验

系统核对候选文献是否真实存在，以及文献内容能否直接支持拟生成的结论，重点检查：

- PMID、标题、年份和链接是否一致；
- 文献是否直接研究相关疾病、药物和结局；
- 回答中的具体数字是否能在摘要或全文中找到；
- 命中的内容是否只是背景、方法或参考文献中的间接提及；
- 引用编号与页面展示的证据编号是否一致。

Trace示例：

```json
{
  "step": 2,
  "action": "verify_citation",
  "result": {
    "checked": 5,
    "valid_citations": 4,
    "direct_support": 2,
    "unsupported_claims": 1,
    "problems": [
      "一项具体下降百分比未在当前摘要中找到"
    ]
  }
}
```

### 第3步：回答或结构化拒答

系统根据核验结果作出以下决策之一：

1. **正常回答**：存在足够的直接证据；
2. **有限回答**：证据支持结论方向，但无法支持精确数值或强结论；
3. **结构化拒答**：证据数量、相关性或质量不足；
4. **建议补检**：缺少指定研究类型或关键人群、干预、对照、结局信息。

Trace示例：

```json
{
  "step": 3,
  "action": "answer",
  "decision": "partial_answer",
  "reason": "疗效方向得到直接证据支持，但高等级证据中的具体效应值不完整"
}
```

## 三、Agent与普通固定流水线的区别

普通流水线每次都按相同顺序执行：

```text
生成检索词
→ 检索
→ 排序
→ 生成回答
```

检索—核验 Agent 会根据中间结果选择后续动作。例如：

```text
第一次检索证据充分
→ 核验引用
→ 正常回答
```

或者：

```text
第一次检索证据不足
→ 改写检索式并补检
→ 核验后有限回答或拒答
```

为了满足“≤3步”，建议当前项目采用最清晰、最容易评估的固定三步框架：

```text
第1步：混合检索
第2步：引用核验
第3步：回答或结构化拒答
```

系统仍可在每一步内部调用多个受控组件，但对外只展示三个主要阶段。

## 四、Trace页面建议展示内容

前端可以增加“执行轨迹”折叠区域：

```text
执行轨迹

步骤1：混合检索
检索式：bempedoic acid LDL-C reduction...
找到15条候选，相关性过滤后保留5条。

步骤2：引用核验
核验5篇文献；4篇元数据有效；2篇直接支持核心结论；
1项具体数值未得到当前原文支持。

步骤3：回答决策
决策：有限回答
原因：疗效方向得到支持，但定量证据不完整。
```

Trace中不应展示：

- API密钥；
- 用户隐私信息；
- 完整系统Prompt；
- 大模型隐藏的详细思维链；
- 与用户任务无关的内部配置。

## 五、适合当前项目的动作集合

Agent可以使用以下受控动作：

| 动作 | 作用 |
|---|---|
| `search` | 调用本地证据库与在线数据库进行混合检索 |
| `refine_search` | 在证据不足时改写检索式并补检 |
| `verify_citation` | 核验文献身份、引用内容及结论支持关系 |
| `answer` | 生成有直接证据支持的医学回答 |
| `partial_answer` | 对证据不完整的问题作有限回答 |
| `refuse` | 输出结构化拒答，并说明证据缺口和补检建议 |

## 六、当前项目已有基础

当前系统已经具备：

- 本地医学文献知识库；
- PubMed等在线检索；
- BM25与向量混合检索；
- 可选Rerank精排；
- 相关性和数量门控；
- 强制引用；
- 结构化拒答；
- 多轮追问界面雏形。

仍需补充：

- 独立的引用核验函数；
- 三步Agent控制器；
- 每一步的结构化Trace；
- 前端Trace展示；
- 至少8道固定问题的评估表和拒答记录。

## 七、推荐实施顺序

1. 先实现 `verify_citation`，核验PMID、元数据和结论支持关系；
2. 统一回答引用编号与右侧证据卡片编号；
3. 将检索、核验、回答或拒答封装成三个步骤；
4. 每一步生成结构化Trace；
5. 在Streamlit页面增加“执行轨迹”折叠面板；
6. 使用固定测试题保存评估结果和拒答记录。

## 八、最终目标

该挑战项的重点不是增加更多检索次数，而是让系统的回答过程具备以下特征：

- **可运行**：能自动完成检索、核验和回答；
- **可控制**：主要步骤不超过三个；
- **可核验**：结论与引用之间有明确对应关系；
- **可拒答**：证据不足时不强行生成医学结论；
- **可审计**：通过Trace说明系统检索到什么、核验了什么，以及为什么回答或拒答。
