# 多轮追问实现

## 核心链路

```
用户提问 Q1 → 生成答案 A1 → 提取摘要(200字) → 存入 history
用户追问 Q2 → 带上 history → 消解"它/这个" → 生成答案 A2 → 追加 history
```

## 三步流程

```
① 历史存储
  st.session_state.conversation_history
  = [{"question": Q1, "summary": A1前200字}, ...]

② 历史注入（两个入口）
  ┌─ PubMed检索词生成：query_builder.py
  │   "Previous conversation: ..."
  │   帮助LLM在英文检索词中体现上下文
  └─ 最终回答生成：rag.py build_prompt()
      "以下是之前的对话...请注意指代关系"
      历史注入到 System Prompt 的对话历史段落

③ 历史追加
  每轮结束后，将 Q + A摘要 写入 history
  下一轮自动携带全部历史
```

## 指代消解示例

```
Q1: 高血压患者应该选择哪种降压药？      → 无历史
Q2: 它对肾脏有保护作用吗？              → 带 Q1 历史，"它" → 消解为"ACEI/ARB"
Q3: 这个药的副作用有哪些？              → 带 Q1+Q2 历史
```

## 前端交互

```
侧边栏 ─ [多轮追问模式] toggle
       ─ 已记录 N 轮对话
       ─ [新对话] 按钮 → 清空历史

主页面 ─ ↓ 对话历史 折叠面板
         第1轮  Q: ...  A: ...
         第2轮  Q: ...  A: ...
```

## Prompt 示例

```
## 对话历史

以下是之前的对话，当前问题是上一轮的追问，
请注意问题中的指代关系
（如"它"、"这个"等可能指代历史中的医学主题）：

Q: 高血压患者应该选择哪种降压药？
A: 根据ESH指南，首选的降压药物包括ACEI、ARB...

---

用户问题：
它对肾脏有保护作用吗？
```

## 涉及文件

| 文件 | 职责 |
|------|------|
| `app.py` | UI toggle、session_state 存储、历史格式化 |
| `query_builder.py` | PubMed检索词生成时带历史做指代消解 |
| `rag.py` `build_prompt()` | System Prompt 中注入对话历史段落 |
