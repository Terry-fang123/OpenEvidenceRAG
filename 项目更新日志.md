# 项目更新日志

## 2026-08-12 下午：拒答机制优化 + 三步Agent + 第二轮评估 + 详细报告

---

### 功能五：证据门控拒答机制优化

#### 问题

上午评估发现以下问题：
- **Q01 误拒**："指南或研究依据"被 `detect_requested_types` 误判为强制要求指南
- **Q09 漏判**：`帮我开个降压药` 中的 "开" 和 "药" 之间有 "个降压"，正则 `帮我.{0,12}开药` 匹配不到
- **闸门 2 过度严格**：`min_reliable_evidence=2` 且大部分 PubMed 摘要 publication_types 为 "Journal Article"，导致 1 条 PubMed 文献就触发拒答

#### 改动

| # | 文件 | 位置 | 修改 |
|:--:|------|------|------|
| ① | `evidence_gate.py` | `evaluate_evidence()` | `min_reliable_evidence`: 2 → **1** |
| ② | `evidence_gate.py` | `detect_requested_types()` | "指南" → 仅匹配 "只用指南/指南证据/指南推荐" 强限定语境 |
| ③ | `evidence_gate.py` | `exceeds_clinical_boundary()` | 新增 `(帮\|替\|给)我.{0,6}开.{0,6}药` 模式 |

#### 效果

- Q01（高血压长期吃药）从误拒 → **正常回答**
- Q09（帮我开个降压药）从漏判 → **正确拒答**
- 预期一致性：70% → **80%**

---

### 功能六：三步检索—核验 Agent + Trace

#### 功能

赛道一挑战任务，将原有 6 步串行流程压缩为 3 步，嵌入自动核验和全链路 Trace：

```
Step 1: 混合检索
  generate_pubmed_query → evidence_agent()（双库检索+生成预答案）

Step 2: 引用核验 Agent（三层）
  层次1: 元数据核验（PMID/NCT ID 格式+标题非空，零成本）
  层次2: 主题相关性（向量余弦相似度，复用现有模型）
  层次3: LLM 结论支撑度判定（一次调用，批量判断）

Step 3: 决策
  verified ≥ 3  → 直接使用 step1 预答案
  verified ≥ 1  → 附"证据不完整"声明的部分回答
  verified == 0 → 结构化拒答 + 建议补检
```

#### 数据结构

| 类 | 字段 |
|------|------|
| `CitationEntry` | evidence_index, pmid, source_id, title, metadata_valid, topic_relevant, claim_supported, is_background, issues |
| `VerificationReport` | total_checked, valid_metadata, topic_relevant, claim_supported, unsupported_claims, per_citation, summarize() |
| `StepTrace` | step, action, input_summary, output_summary, decision_note, elapsed_ms, errors |
| `ChallengeTrace` | trace_id, question, steps, final_decision, total_elapsed_ms, to_json() |

#### 改动文件

| 文件 | 改动 |
|------|------|
| `challenge_agent.py` | **新增**。三步控制器 `run_3step_agent()`、核验 Agent `step2_verify()`、批量评估 `run_challenge_eval()` |
| `app.py` | 侧边栏新增「三步Agent模式」开关（默认关闭）；搜索结果底部新增「执行轨迹」折叠面板 |

#### 前端效果

三步 Agent 模式下，搜索结果页底部出现 "▼ 执行轨迹" 折叠面板：

```
🔍 步骤 1：search  3200ms
  query: bempedoic acid LDL-C reduction...
  initial_count: 15  (PubMed 5 + 试验 3 + 指南 7)
  initial_pmids: [42236342, ...]
  selected_pmids: [42236342, ...]
  filtered_pmids: [12345678, ...]

✅ 步骤 2：verify_citation  4500ms
  checked: 8 | valid_metadata: 7 | direct_support: 4
  rejected: [PMID:xxx 语义相关度低 (sim=0.34)]

📝 步骤 3：answer  3800ms
  decision: answer
  reason: 8篇中4篇直接支撑...
```

#### 零侵入设计

- 开关关闭时完全走原有 `evidence_agent()` 六步流程
- `run_3step_agent()` 内部调现有 `evidence_agent` / `generate_pubmed_query` / `rag` 等模块
- 不修改任何现有模块

#### 关键修复

step1 的 `evidence_agent()` 已生成完整回答，step3 直接复用而不重新调用 `answer_question()`，避免二次生成缺 `local_pdf_query` 导致检索质量下降。

---

### 功能七：向量数据库修复

#### 问题

ChromaDB HNSW 索引损坏，报错 `InternalError: Error constructing hnsw segment reader`。

#### 原因

之前 `atexit._clear()` 阻止了 ChromaDB 正常关闭，导致索引写入不完整。

#### 改动

| 文件 | 改动 |
|------|------|
| `vector_db.py` | `atexit` 修复：提前占位注册，保证 ChromaDB 正常关闭 |
| `chroma_db/` | 删除损坏索引，重启后自动重建 |

#### 影响

`chroma_db` 在线数据丢失一次（约 500 条 API 检索记录），下次查询时自动重新拉取。`evidence_store`（983 篇本地知识库）不受影响。

---

### 功能八：第二轮评估 + 详细报告

#### 运行结果

| 指标 | 上午 | 下午 | 变化 |
|------|:--:|:--:|:--:|
| 正常回答 | 7 | 7 | 持平 |
| 预期一致 | 7/10 (70%) | **8/10 (80%)** | ▲10% |
| 引用有效率 | 100% | 100% | 持平 |
| 平均耗时 | 25.1s | 24.6s | — |

#### 关键变化

| 题号 | 上午 | 下午 | 说明 |
|:--:|------|------|------|
| Q01 | 误拒 | **回答** ✓ | 指南脱敏生效 |
| Q09 | 漏判 | **拒答** ✓ | 新模式生效 |
| Q04 | 回答 | **拒答** ✗ | API 波动（地中海饮食检索失败） |

#### 新增报告

`eval_detail_report.md` — 详细评估报告，包含每道题的完整回答原文、逐条引用证据表（PMID+标题）、四维质量评价（完整性/准确性/诚实性/安全性）。

---

## 2026-08-12 上午：评估框架 + 质检工作流 + Rerank 精排

- **改动范围**：新增评估框架 `eval_runner.py`、质检工作流 `qa_pipeline.py`、`reranker.py`、Streamlit 质检面板

---

### 功能一：评估框架 (`eval_runner.py`)

#### 功能

面向赛道一"临床证据"必做要求，对系统回答进行批量评估：
- **10 道预设测试题**：覆盖正常检索、特定证据类型要求、临床边界拒答、证据不足拒答
- **引用核验**：从 LLM 回答中提取 `[N]` 引用，逐一验证引用编号是否有效
- **拒答记录**：检测 GateDecision 中的拒答原因码
- **CSV 评估报告**：10 题逐题结果，字段包含引用有效率、拒答原因、耗时等

#### 改动文件

| 文件 | 改动 |
|------|------|
| `eval_runner.py` | **新增**。`EvalRecord` 数据类、`extract_citation_numbers()`、`verify_citations()`、`detect_refusal()`、`run_evaluation()` |

#### 当前运行结果

| 指标 | 数值 |
|------|------|
| 引用有效率 | 100%（32/32，零伪造引用） |
| 预期-实际一致 | 7/10 |
| 已知问题 | Gate 过度敏感导致误拒；临床边界正则漏判 `帮我开个X药` |

---

### 功能二：质检工作流 (`qa_pipeline.py`)

#### 功能

赛道一进阶·方向三，对单次回答做四维后处理质检：
- **① 完整性检查**：回答是否覆盖 Prompt 要求的 8 个段落
- **② 安全性检查**：正则扫描越界表述（具体剂量、停药指令、诊断结论等）
- **③ 证据匹配度**：向量模型计算问题与每条 evidence 的余弦相似度，标记弱匹配
- **④ 稳定性评估**：同题跑 2 遍，对比输出语义相似度 + PMID 引用重叠率

#### 改动文件

| 文件 | 改动 |
|------|------|
| `qa_pipeline.py` | **新增**。四个检查函数 `check_completeness()` / `check_safety()` / `check_evidence_relevance()` / `check_stability()`，以及批量包装 `run_qa_batch()` |

#### 数据结构

- `CompletenessResult`：段落覆盖率 + 缺失列表
- `SafetyResult`：是否安全 + 违规上下文列表
- `RelevanceResult`：均值/中位数/最低分 + 弱匹配序号列表
- `StabilityResult`：语义相似度 + PMID 重叠率
- `QAResult`：四项合一

---

### 功能三：Streamlit 质检面板

#### 功能

在 Streamlit 前端新增"▼ 质检报告"可折叠区域，位于回答+证据双栏下方、运行日志上方：
- **文字版质检摘要**：根据完整性/安全性/匹配度自动生成三段式自然语言报告 + 总评结论
- **三列指标卡片**：完整性（进度条+缺失项）、安全性（通过/风险数）、证据匹配度（分数+进度条）
- **证据逐条匹配度**：横向卡片，每条证据带分数和颜色条

#### 改动文件

| 文件 | 改动 |
|------|------|
| `app.py` | 第 10 行新增 `from qa_pipeline import ...`；`main()` 中新增第 747-800 行质检报告代码 |

---

### 功能四：Rerank 精排 (`reranker.py`)

#### 功能

在混合检索与 RRF 粗排之间插入 LLM 驱动的 Rerank 层，对候选文献逐篇打分（0-10），最终只保留高分文献喂给 RAG Prompt：

```
混合检索 (topk × 2 候选)
    → RRF 粗排 (取 topk × 3)
    → Rerank 精排 (LLM 逐篇打分 0~10)
    → metadata 证据等级加权 (final_score = rerank_score × weight)
    → 按 final_score 降序取 topk
```

#### 完整流水线（`rag.py` → `_rerank_pipeline()`）

```python
def _rerank_pipeline(
    question: str,
    evidence_list: list[dict],
    topk: int,
) -> list[dict]:
    """Rerank 完整流水线：文本块 → 文献级聚合 → LLM 打分 → 加权 → 排序。"""
    if not evidence_list:
        return []

    # 1. 文献级聚合：同一 PMID 的多个 chunk 合并为一条
    documents = _aggregate_to_document_level(evidence_list)

    if len(documents) <= topk:
        return documents[:topk]  # 候选太少，不做 Rerank

    # 2. LLM 逐篇打分 0-10
    from reranker import rerank_documents

    documents = rerank_documents(
        question=question,
        documents=documents,
    )

    # 3. metadata 证据等级加权
    documents = _apply_metadata_weight(documents)

    # 4. 按 final_score 排序取 Top-K
    documents.sort(
        key=lambda d: d.get("final_score", 0.0),
        reverse=True,
    )
    return documents[:topk]
```

#### metadata 证据等级加权（`rag.py`）

根据 publication_types 对 LLM 打分做按证据等级加权：

| 证据类型 | 权重 | 说明 |
|------|:--:|------|
| Meta-Analysis | 1.3 | 最高级别证据 |
| Systematic Review | 1.2 | 系统评价 |
| Practice Guideline | 1.15 | 临床实践指南 |
| Randomized Controlled Trial | 1.1 | RCT |
| Guideline | 1.1 | 一般指南 |
| 其他类型 | 1.0 | 默认不加权 |

```python
_METADATA_WEIGHTS = {
    "Meta-Analysis": 1.3,
    "Systematic Review": 1.2,
    "Randomized Controlled Trial": 1.1,
    "Practice Guideline": 1.15,
    "Guideline": 1.1,
}

def _apply_metadata_weight(
    evidence_list: list[dict],
) -> list[dict]:
    """对每条证据的 rerank_score 乘以 metadata 权重。"""
    for evidence in evidence_list:
        score = evidence.get("rerank_score", 5.0)
        weight = _get_metadata_weight(evidence)
        evidence["metadata_weight"] = weight
        evidence["final_score"] = score * weight
    return evidence_list
```

**示例**：某 Meta-Analysis 被 LLM 打 8 分 → final_score = 8 × 1.3 = 10.4，排位显著提升。

#### LLM 打分 Prompt（`reranker.py`）

```python
prompt = f"""
You are a clinical evidence relevance reviewer.

User question:
{question}

Score each document (0-10) on how well it directly answers the question:
- 0-2: completely irrelevant
- 3-5: topic-related but does not directly answer
- 6-8: directly relevant
- 9-10: precisely answers

Return ONLY a JSON object (no markdown, no extra text):
{{"scores": [9, 7, 3, ...]}}

One score per document, in the same order as listed above.

{doc_blocks}
"""
```

**设计要点**：
- 所有候选文献一次调用打分，避免多次 API 调用
- 每篇仅传标题 + 前 500 字符摘要控 token
- temperature=0.0，确保打分一致性

#### 改动文件

| 文件 | 改动 |
|------|------|
| `reranker.py` | **新增**。`rerank_documents()` 函数，LLM 批量打分 + JSON 解析 + 失败兜底（中性分 5.0） |
| `rag.py` | **新增** `_rerank_pipeline()`、`_apply_metadata_weight()`、`_METADATA_WEIGHTS`；`answer_question()` 中调用 |
| `main.py` | `evidence_agent()` 新增 `use_rerank` 参数，透传给 `answer_question()` |
| `app.py` | 侧边栏新增「Rerank 精排」开关（默认关闭） |

#### 容错设计

- 无 API Key → 所有文档赋 5.0 中性分
- LLM 调用失败 → 所有文档赋 5.0
- JSON 解析失败 → 打印 LLM 原始输出，所有文档赋 5.0
- 中性分 5.0 表示"不确定"，不偏向任何一方

---

## 版本信息

- **日期**：2026-08-11
- **改动范围**：检索层 + LLM 调用链路 + Streamlit UI

---

## 功能一：多轮追问模式

### 问题

当连续追问时（如 Q101: "SGLT2 抑制剂有什么心血管证据？" → Q102: "它对肾脏有保护作用吗？"），系统无法理解 Q102 中的"它"指代什么，因为每次问答都是独立的，没有上下文记忆。

### 解决方案

在 LLM 调用链路的每一层增加 `history` 参数，将对话历史注入 Prompt，让 LLM 自行完成指代消解。同时在 Streamlit UI 中增加多轮追问开关和对话历史管理。

### 改动文件

#### `query_builder.py`

| 函数 | 改动 |
|---|---|
| `build_query_prompt()` | 新增 `history` 参数。有历史时，在 Prompt 前附加 `Previous conversation:` + 历史内容，并提示 LLM 结合上文消解指代 |
| `call_query_model()` | 新增 `history` 参数，透传给 `build_query_prompt()` |
| `generate_pubmed_query()` | 新增 `history` 参数，透传给 `call_query_model()` |

**核心代码** — `build_query_prompt()`：

```python
def build_query_prompt(question: str, history: str = "") -> str:
    history_block = ""

    if history:
        history_block = (
            "Previous conversation:\n"
            f"{history}\n"
            "---\n"
            "The current question may contain pronouns (e.g. 'it', 'they') "
            "that refer to concepts mentioned above. "
            "Use the previous conversation to resolve these references "
            "before generating the query.\n"
        )

    return f"""
You are helping build a clinical evidence retrieval system.

Convert the user's Chinese clinical question into one concise English PubMed search query.

Requirements:
1. Output the query only. Do not explain.
2. Use English biomedical terms.
3. Prefer core concepts: disease, intervention, comparator, outcome, evidence type.
4. Use simple PubMed-friendly Boolean operators only when helpful.
5. Do not include markdown, numbering, citations, or Chinese text.
6. Keep the query under 25 words.

{history_block}User question:
{question}
""".strip()
```

#### `rag.py`

| 函数 | 改动 |
|---|---|
| `build_fallback_answer()` | 新增 `history` 参数，在摘录模式中也展示对话历史段落 |
| `build_prompt()` | 新增 `history` 参数，在 Prompt 顶部插入 `## 对话历史` 段，提示 LLM 注意指代关系 |
| `answer_question()` | 新增 `history` 参数，透传给 `build_prompt()` 和所有 `build_fallback_answer()` 调用（共 8 处） |

**核心代码** — `build_prompt()`：

```python
def build_prompt(question: str, evidence_list: list[dict], history: str = "") -> str:
    context = build_evidence_context(evidence_list)
    reference_summary = build_reference_summary(evidence_list)

    history_block = ""
    if history:
        history_block = f"""
## 对话历史

以下是之前的对话，当前问题是上一轮的追问，请注意问题中的指代关系（如"它"、"这个"等可能指代历史中的医学主题）：

{history}

---

"""

    prompt = f"""
你是一名谨慎的临床证据助手。

请只根据下面提供的医学证据回答问题。

{history_block}用户问题：

{question}

检索到的医学证据：

{context}

证据编号与来源：

{reference_summary}

必须遵守以下要求：

1. 只能使用上面提供的证据，不允许编造医学事实。
2. 不允许编造作者、论文、期刊、指南、PMID、NCT ID、指南 ID、统计数字或治疗阈值。
3. 每个关键结论都必须标注对应证据编号，例如 [1]、[2]。
4. 必须区分证据类型：PubMed 在线文献、本地PDF全文、ClinicalTrials.gov 临床试验登记、指南/共识。
5. 指南/共识可以说明推荐方向，但不得把它当作针对个人的诊断或处方。
6. 如果证据不足、证据与问题不完全相关，必须明确说明，不要强行得出结论。
7. 区分"生活方式干预"和"药物治疗"。
...
"""
```

#### `main.py`

| 函数 | 改动 |
|---|---|
| `evidence_agent()` | 新增 `history` 参数，透传给 `generate_pubmed_query()` 和 `answer_question()` |

#### `app.py`

| 位置 | 改动 |
|---|---|
| `RunSettings` | 新增 `multi_turn: bool` 字段 |
| `render_sidebar()` | 新增"多轮追问模式"开关 + "新对话"按钮 |
| `run_pipeline()` | 构建历史文本、传入 `evidence_agent()`、记录本轮 Q&A 摘要 |
| `main()` | 新增可折叠的"对话历史"区域 |

**核心代码** — 侧边栏多轮追问开关：

```python
# ---- 多轮追问模式 ----
st.divider()
st.subheader("多轮追问")

multi_turn = st.toggle(
    "多轮追问模式",
    value=False,
    help=(
        "开启后，系统会记住之前的问答，"
        "自动理解下一轮问题中的指代关系。"
    ),
)

# 初始化对话历史
if "conversation_history" not in st.session_state:
    st.session_state.conversation_history = []

if multi_turn and st.session_state.conversation_history:
    st.caption(
        f"已记录 {len(st.session_state.conversation_history)} 轮对话"
    )

if st.button(
    "新对话",
    use_container_width=True,
    disabled=not multi_turn,
):
    st.session_state.conversation_history = []
    st.rerun()
```

**核心代码** — `run_pipeline()` 中构建历史并记录本轮：

```python
# ---- 构建多轮对话历史 ----
history = ""

if settings.multi_turn and st.session_state.conversation_history:
    history_items = []
    for item in st.session_state.conversation_history:
        history_items.append(
            f"Q: {item['question']}\nA: {item['summary']}"
        )
    history = "\n---\n".join(history_items)

# ---- 记录本轮对话到历史 ----
if settings.multi_turn and answer:
    # 取回答的前 200 个字符作为摘要
    answer_text = answer or ""
    summary = answer_text.split("## 简短回答")[-1] if "## 简短回答" in answer_text else answer_text
    summary = summary.split("\n\n")[0] if "\n\n" in summary else summary
    summary = summary.strip()[:200]

    st.session_state.conversation_history.append(
        {
            "question": settings.question,
            "summary": summary,
        }
    )
```

### 使用方式

1. 在 Streamlit 侧边栏打开 **"多轮追问模式"** 开关
2. 输入第一轮问题，点击 **"运行检索与回答"**
3. 输入第二轮追问（可用"它"/"这个"等指代词）
4. 系统自动结合上文生成正确检索词和回答
5. 点击 **"新对话"** 清空历史，重新开始

### 数据流

```
st.session_state.conversation_history
  (Q1 + A1_summary, Q2 + A2_summary, ...)
  → 拼接为 history 文本
  → query_builder (检索词消歧)
  → evidence_agent → answer_question
  → build_prompt / call_openai (回答消歧)
  → 新增 Q&A 摘要追加到 conversation_history
```

---

## 功能二：BM25 + 向量混合检索

### 改动动机

原有检索仅依赖纯向量语义匹配（ChromaDB embedding），对于以下场景表现不佳：
- 精确医学术语匹配（如"SGLT2 inhibitor"、"LDL-C"）
- 缩写和同义术语的区分
- 用户问题与文档用词不一致时的召回

引入 BM25 关键词检索与向量检索双路融合，互补各自的不足。

### 技术方案：加权 RRF（Reciprocal Rank Fusion）

```
用户查询
    │
    ├── 向量检索 (topk × 2)          BM25 关键词检索 (topk × 2)
    │         │                              │
    │    dense_rank 1,2,3...           bm25_rank 1,2,3...
    │         │                              │
    └─────────┴────── RRF 融合 ─────────────┘
                        │
        score = w/(60+rank_dense) + 1/(60+rank_bm25)
                        │
                 按 RRF 分数降序
                        │
                   最终 topk
```

- RRF 参数 K=60，业界默认值
- **w 为向量权重**（默认 1.0），通过前端滑块可调（0.5~1.5）；> 1.0 偏向语义，< 1.0 偏向关键词

### 改动文件

#### `requirements.txt`

```
+ rank-bm25
```

纯 Python 库，无额外系统依赖。

#### `vector_db.py`

| 位置 | 改动 |
|---|---|
| import | 新增 `from rank_bm25 import BM25Okapi` |
| 全局变量 | 新增 `_bm25_index`、`_bm25_doc_ids`、`_bm25_corpus` |
| `_rebuild_bm25()` | **新增函数**。从 Chroma 全量读取文档 → 分词 → 构建 BM25 索引 |
| `_vector_search_only()` | **新增函数**。封装改动前的纯向量检索逻辑 |
| `delete_existing_document()` | 删除文档后追加 `_rebuild_bm25()` 调用 |
| `add_document()` | 文档写入后追加 `_rebuild_bm25()` 调用 |
| `search_with_details()` | 新增 `retrieval_mode`（hybrid/vector_only）和 `dense_weight` 参数 |

**核心代码** — BM25 索引构建：

```python
# 模块级全局变量
_bm25_index: Optional[BM25Okapi] = None
_bm25_doc_ids: list[str] = []
_bm25_corpus: list[list[str]] = []

def _rebuild_bm25() -> None:
    """从 Chroma 中读取全部文档，重新构建 BM25 索引。"""
    global _bm25_index, _bm25_doc_ids, _bm25_corpus

    try:
        all_docs = collection.get(include=["documents"])
        documents = all_docs.get("documents", [])
        doc_ids = all_docs.get("ids", [])
    except Exception:
        _bm25_index = None
        _bm25_doc_ids = []
        _bm25_corpus = []
        return

    if not documents:
        _bm25_index = None
        _bm25_doc_ids = []
        _bm25_corpus = []
        return

    # 分词：按空白和标点切分
    tokenized_corpus = [
        re.findall(r"\w+", doc.lower())
        for doc in documents if doc
    ]

    # 过滤掉分词后为空的文档
    valid_indices = [idx for idx, tokens in enumerate(tokenized_corpus) if tokens]
    if not valid_indices:
        _bm25_index = None
        _bm25_doc_ids = []
        _bm25_corpus = []
        return

    _bm25_doc_ids = [doc_ids[idx] for idx in valid_indices]
    _bm25_corpus = [tokenized_corpus[idx] for idx in valid_indices]
    _bm25_index = BM25Okapi(_bm25_corpus)

    print(f"BM25 索引已重建，共 {len(_bm25_corpus)} 个文档。")
```

**核心代码** — `search_with_details()` 签名和 RRF 融合：

```python
def search_with_details(
    query: str,
    topk: int = 3,
    retrieval_mode: str = "hybrid",
    dense_weight: float = 1.0,
) -> list[dict]:
    """
    retrieval_mode:
        hybrid：BM25 + 向量 RRF 混合检索（默认）
        vector_only：纯向量检索
    dense_weight:
        向量路权重系数，BM25 路固定为 1.0。
        > 1.0 偏向语义，< 1.0 偏向关键词。
    """

    # ---- 纯向量模式 ----
    if retrieval_mode == "vector_only":
        return _vector_search_only(cleaned_query, topk, document_count)

    # ---- 混合检索模式 ----
    candidate_count = max(topk * 2, topk)

    # 1. 向量检索 → dense_result
    # 2. BM25 检索 → bm25_results

    # ---- 3. RRF 融合 ----
    RRF_K = 60
    fused_scores: dict[str, float] = {}

    for chunk_id, info in dense_index.items():
        rrf = dense_weight / (RRF_K + info["dense_rank"])
        if chunk_id in bm25_results:
            rrf += 1.0 / (RRF_K + bm25_results[chunk_id]["bm25_rank"])
        fused_scores[chunk_id] = rrf

    for chunk_id, info in bm25_results.items():
        if chunk_id not in fused_scores:
            fused_scores[chunk_id] = 1.0 / (RRF_K + info["bm25_rank"])

    # 按 RRF 分数降序排列
    sorted_chunk_ids = sorted(fused_scores, key=fused_scores.get, reverse=True)
```

#### `rag.py`

| 位置 | 改动 |
|---|---|
| `answer_question()` | 新增 `retrieval_mode` 和 `dense_weight` 参数，透传给 `search_with_details()` |

#### `main.py`

| 位置 | 改动 |
|---|---|
| `evidence_agent()` | 新增 `retrieval_mode` 和 `dense_weight` 参数，透传给 `answer_question()` |

#### `app.py`

| 位置 | 改动 |
|---|---|
| `RunSettings` | 新增 `retrieval_mode: str` 和 `dense_weight: float` 字段 |
| `render_sidebar()` | 新增"检索方式"单选按钮和"向量权重"滑块 |
| `run_pipeline()` | 读取配置后传入 `evidence_agent()` |

### 前端对比功能

1. **检索方式**：切换 `混合检索 (BM25+向量)` / `纯向量检索`，重跑同一问题即可对比两边的证据列表
2. **向量权重**：仅在混合检索模式生效，滑动调整向量路权重（> 1.0 偏向语义，< 1.0 偏向关键词）

### 测试验证

```
$ python vector_db.py

--- 结果 1 ---
来源：pubmed_42574909  距离：0.7355  ← 向量路召回
--- 结果 2 ---
来源：pubmed_42170981  距离：None     ← BM25 路召回（语义不匹配但关键词命中 lipid）
--- 结果 3 ---
来源：sprint_trial      距离：0.7428  ← 向量路召回
```

结果 2 的 `distance=None` 说明它仅被 BM25 路命中，这正是混合检索的预期行为。

---

## 维护注意事项

### 多轮追问
- 对话历史存储在 `st.session_state.conversation_history`，刷新页面后丢失
- 回答摘要是从 LLM 输出中截取"简短回答"段落的前 200 个字符

### 混合检索
- 首次运行时 BM25 索引为空（`None`），自动退化为纯向量检索
- `add_document()` 后会触发全量 BM25 重建，文档量极大时可能有性能影响；当前项目规模（数百条）完全可控
- 如需更换分词策略，修改 `_rebuild_bm25()` 中的 `re.findall(r"\w+", ...)` 即可
- 纯向量检索模式不会构建 BM25 索引，切换回混合模式时依赖已存在的索引

---

## 功能三：Rerank 精排（已于 2026-08-12 实现，详见上文）

### 问题

混合检索（BM25 + 向量 RRF）能快速召回一批候选（通常 topk × 2 篇），但排序仅依赖简单的排名融合，无法精准区分"真正直接相关"和"仅关键词/语义碰巧匹配"的结果。例如：

- 用户问"依折麦布联合他汀降脂的证据"
- 检索可能返回依折麦布 Meta 分析、REDUCE-IT 研究（鱼油不是依折麦布）、PCSK9 研究（相邻主题）、甚至完全无关文献
- 需要一层精排把假相关文献降级或排除

### 解决方案

在现有混合检索和 RRF 融合之间插入一个 **LLM 驱动的 Rerank 层**：

```
混合检索 (topk × N 候选)
    → RRF 粗排 (取更多候选，topk × 3)
    → Rerank 精排 (LLM 逐篇打分 0~10)
    → 最终 topk
```

### 新增模块：`reranker.py`

```python
def rerank_candidates(
    question: str,
    candidates: list[dict],
    topk: int,
) -> list[dict]:
    """
    将候选文档批量发给 LLM，
    让 LLM 同时阅读「问题 + 标题 + 摘要」逐篇打分 0~10，
    按分数降序返回 topk。
    """
```

### Prompt 设计

一次调用传入所有候选，LLM 同时逐篇判断，避免多次 API 调用：

```
你是临床证据相关性评审员。

用户问题：{question}

请对以下文献逐篇打分（0-10）：
- 0-2: 完全无关
- 3-5: 主题相关但不直接回答问题
- 6-8: 直接相关
- 9-10: 精准回答

只输出 JSON 数组：
[{"doc_id": "...", "score": 9}, ...]

文献 1: 标题... 摘要...
文献 2: 标题... 摘要...
...
```

### 改动范围

| 文件 | 改动 |
|---|---|
| `reranker.py` | **新增**。`rerank_candidates()` 函数 |
| `vector_db.py` | `search_with_details()` 新增 `use_rerank` 参数；开启时候选池扩到 `topk×3`，RRF 粗排后调用 `rerank_candidates()` |
| `rag.py` | `answer_question()` 新增 `use_rerank`，透传 |
| `main.py` | `evidence_agent()` 新增 `use_rerank`，透传 |
| `app.py` | 侧边栏新增「Rerank 精排」开关（默认关闭） |

### 优缺点

| 优点 | 缺点 |
|---|---|
| 精准消除假相关，不相关文献不会再混入 | 多一次 LLM 调用，响应变慢 |
| 批量打分，一次 API 搞定所有候选 | 候选越多 token 消耗越大 |
| 前端开关可按需开启，默认不影响速度 | — |
