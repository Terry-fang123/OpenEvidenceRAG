# OpenEvidence 项目架构文档

## 1. 项目定位

复刻商业产品 [OpenEvidence](https://www.openevidence.com) 的核心工作流：将中文医学问题自动转换为可追溯、有引用的循证回答。**课程项目 / 技术演示 MVP，非临床工具。**

---

## 2. 技术栈

| 层次 | 技术选型 |
|---|---|
| 语言 | Python 3.11 |
| LLM | DeepSeek（OpenAI 兼容接口，`rag.py` 中通过 `OPENAI_BASE_URL` 配置） |
| 向量数据库 | ChromaDB（本地 SQLite 持久化，`chroma.sqlite3`） |
| 嵌入模型 | `paraphrase-multilingual-MiniLM-L12-v2`（384 维，中英跨语言） |
| 关键词检索 | `rank-bm25`（BM25Okapi） |
| Web UI | Streamlit（`app.py`）+ NiceGUI（`nicegui_app.py`） |
| 数据源 | PubMed E-utilities API、ClinicalTrials.gov v2 API、本地指南 JSON、本地 PDF |

---

## 3. 完整数据流

```
                          ┌─────────────────────────────────────┐
                          │          离线数据构建（一次性）           │
                          │                                     │
                          │  team_evidence.json  (500 PubMed)   │
                          │  local_pdf_store/    (483 本地PDF)  │
                          │  clinical_guidelines.json           │
                          │         │                           │
                          │  ingest_team_evidence.py            │
                          │  build_pdf_store.py                 │
                          │  unify_evidence_store.py            │
                          │         │                           │
                          │         ▼                           │
                          │  ┌─────────────────────┐            │
                          │  │  chroma.sqlite3     │            │
                          │  │  (ChromaDB 本地持久化)│            │
                          │  │  14,000+ 文本块      │            │
                          │  └─────────────────────┘            │
                          └─────────────────────────────────────┘
                                          │
                                          │ 运行时只读查询
                                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│                           运行时数据流                                 │
│                                                                     │
│  用户中文问题                                                        │
│       │                                                             │
│  ┌────▼──────────────┐                                              │
│  │ 1. query_builder  │  DeepSeek 中文→英文 PubMed 检索词               │
│  │    (query_builder  │  history 参数支持多轮追问指代消解               │
│  │     .py)          │                                              │
│  └────┬──────────────┘                                              │
│       │                                                             │
│  ┌────▼──────────────────────────────────────────┐                  │
│  │ 2. 证据采集（并行）                              │                  │
│  │                                              │                  │
│  │  pubmed.py ──→ PubMed E-utilities API        │  实时网络请求     │
│  │       ──→ 搜索结果写入 ChromaDB               │                  │
│  │                                              │                  │
│  │  clinical_trials.py ──→ ClinicalTrials.gov  │  实时网络请求     │
│  │       ──→ 搜索结果写入 ChromaDB               │                  │
│  │                                              │                  │
│  │  guidelines.py ──→ clinical_guidelines.json │  本地 JSON 匹配   │
│  │       ──→ 匹配结果写入 ChromaDB               │                  │
│  └────┬──────────────────────────────────────────┘                  │
│       │                                                             │
│       │  所有数据汇入同一个 ChromaDB（chroma.sqlite3）                 │
│       ▼                                                             │
│  ┌────▼──────────────┐                                              │
│  │ 3. vector_db.py   │  BM25 + 向量 混合检索（加权 RRF 融合）         │
│  │   search_with_    │  候选池: topk × 2                            │
│  │   details()       │  RRF: w/(60+rank_dense) + 1/(60+rank_bm25)  │
│  │                   │  支持 hybrid / vector_only 模式切换           │
│  └────┬──────────────┘                                              │
│       │                                                             │
│  ┌────▼──────────────┐                                              │
│  │ 4. rag.py         │  构建结构化 Prompt                            │
│  │   build_prompt()   │  ├─ 对话历史（多轮追问时注入）                 │
│  │                   │  ├─ 证据上下文 + 引用编号                      │
│  │                   │  └─ 强制约束（不编造、标注来源）               │
│  │                   │                                              │
│  │   call_openai()    │  → DeepSeek API（OPENAI_BASE_URL 配置）     │
│  │                   │  → 生成中文回答 + [1][2] 引用                 │
│  └────┬──────────────┘                                              │
│       │                                                             │
│  ┌────▼──────────────┐                                              │
│  │ 5. 输出            │  带引用编号 + PMID/NCT ID/指南ID + 证据卡片    │
│  └───────────────────┘                                              │
└─────────────────────────────────────────────────────────────────────┘
```

**关键数据路径总结**：

1. **离线灌库**：`team_evidence.json` + PDF → `ingest_*.py` → ChromaDB
2. **运行时扩库**：PubMed/ClinicalTrials API 实时结果也写入 ChromaDB
3. **查询**：永远只从 ChromaDB 查（混合检索），不再直接读本地文件
4. **生成**：检索结果 → Prompt → DeepSeek → 回答

---

## 4. 模块职责

| 模块 | 核心职责 | 关键函数 |
|---|---|---|
| [main.py](main.py) | **总调度器**。`evidence_agent()` 编排全部流程，透传 `history` / `retrieval_mode` / `dense_weight` | `evidence_agent()`, CLI 入口 |
| [query_builder.py](query_builder.py) | **中文→英文查询生成**。Prompt 注入对话历史做指代消解，LLM 优先 + 关键词兜底 | `build_query_prompt()`, `generate_pubmed_query()` |
| [pubmed.py](pubmed.py) | **PubMed E-utilities API**。搜索 PMID → 获取 XML → 解析标题/摘要/期刊/DOI | `search_pubmed()`, `fetch_pubmed_articles()` |
| [clinical_trials.py](clinical_trials.py) | **ClinicalTrials.gov v2 REST API**。获取试验状态/阶段/入组人数等 | `fetch_clinical_trials()` |
| [guidelines.py](guidelines.py) | **本地指南匹配**。对 `clinical_guidelines.json` 做关键词评分 | `fetch_guidelines()` |
| [vector_db.py](vector_db.py) | **向量存储 + 混合检索**。文本清洗分块、嵌入写入、BM25 索引、RRF 融合检索 | `add_document()`, `search_with_details()`, `_rebuild_bm25()` |
| [rag.py](rag.py) | **RAG 生成层**。Prompt 构建、DeepSeek 调用、优雅降级 | `build_prompt()`, `call_openai()`, `answer_question()` |
| [evidence_gate.py](evidence_gate.py) | **证据门控**。临床边界检测、证据类型要求判断、结构化拒答生成 | `evaluate_evidence()`, `exceeds_clinical_boundary()`, `build_structured_refusal()` |
| [app.py](app.py) | **Streamlit UI**。侧边栏配置、多轮追问模式、三步Agent开关、检索模式切换、质检面板、执行轨迹、证据卡片 | `render_sidebar()`, `run_pipeline()` |
| [evaluation.py](evaluation.py) | **评估框架**。纯 LLM vs RAG 对比评测 | — |
| [eval_runner.py](eval_runner.py) | **评估框架**。10 题批量评估、引用核验、拒答记录、CSV 报告 | `extract_citation_numbers()`, `verify_citations()`, `run_evaluation()` |
| [qa_pipeline.py](qa_pipeline.py) | **质检工作流**。完整性/安全性/证据匹配度/稳定性四维后处理质检 | `check_completeness()`, `check_safety()`, `check_evidence_relevance()` |
| [reranker.py](reranker.py) | **Rerank 精排**。LLM 对候选文献逐篇打分 0-10，筛选高相关性文献 | `rerank_documents()` |
| [challenge_agent.py](challenge_agent.py) | **三步检索—核验 Agent**。三步流水线（检索→核验→决策）+ 三层引用核验 + 全链路 Trace | `run_3step_agent()`, `step2_verify()`, `step3_decide()`, `run_challenge_eval()` |

### 离线数据构建脚本（非运行时）

| 脚本 | 作用 |
|---|---|
| [ingest_team_evidence.py](ingest_team_evidence.py) | 读取 `team_evidence.json`（500 篇 PubMed 摘要）→ 写入 ChromaDB |
| [build_pdf_store.py](build_pdf_store.py) | 解析 `local_pdf_store/`（483 篇 PDF）→ 写入 ChromaDB |
| [unify_evidence_store.py](unify_evidence_store.py) | 统一上述数据源 → 生成 `evidence_store/` 静态快照 |
| [ingest_pdfs.py](ingest_pdfs.py) | 通用 PDF 导入工具 |

---

## 5. 数据存储结构

### ChromaDB（核心，运行时依赖）

- **位置**：`chroma_db/chroma.sqlite3`
- **内容**：所有文献的文本块 + 384 维向量 + 元数据（来源类型、PMID、年份、期刊等）
- **来源**：离线构建的 983 篇 + 运行时 API 新增的
- **大小**：约 14,000+ 个文本块

### 离线快照（一次性构建产物，运行时不用）

- **位置**：`evidence_store/`
- **内容**：`documents.jsonl` + `embeddings.npy` + `manifest.json`
- **作用**：记录离线构建时的完整状态，可被备份/恢复

### 原始数据文件

| 文件 | 说明 |
|---|---|
| `data_sources/team_evidence.json` | 983 篇 PubMed 文献元数据 |
| `guidelines/clinical_guidelines.json` | 本地临床指南 |
| `local_pdf_store_test/` | 测试用 PDF 库 |

---

## 6. 当前功能特性

### 检索层
- **BM25 + 向量混合检索**：加权 RRF 融合，向量权重可调（0.5~1.5）
- **Rerank 精排**：LLM 逐篇打分 0-10，筛选高相关文献（前端开关，默认关闭）
- 前端可切换"混合检索 / 纯向量检索"实时对比效果

### 对话层
- **多轮追问模式**：对话历史注入 Prompt，LLM 自行完成指代消解
- "新对话"按钮一键清空历史

### 输出格式
- 强制引用编号 `[1][2]` + PMID / NCT ID / 指南 ID
- 三类证据源（PubMed / 临床试验 / 指南）分组展示
- LLM 调用失败时优雅降级为证据摘录

### 质检与评估层
- **Streamlit 质检面板**：每次检索后自动展示完整性/安全性/证据匹配度三列指标，含文字版质检摘要
- **批量评估**：`eval_runner.py` 支持 10 题预设测试集，自动核验引用、记录拒答、输出 CSV 报告
- **四维质检**：`qa_pipeline.py` 提供完整性/安全性/证据匹配度/稳定性独立检测函数
- **证据门控**：`evidence_gate.py` 临床边界检测 + 证据类型要求判断 + 结构化拒答（支持 GUIDE/RCT/META_ANALYSIS 等 9 类证据类型）

### 三步 Agent 层
- **三步检索—核验 Agent**：`challenge_agent.py` 将 6 步串行流程压缩为检索→核验→决策 3 步
- **三层引用核验**：元数据(零成本) → 向量语义(低成本) → LLM 结论支撑度
- **全链路 Trace**：每步的输入/输出/耗时/决策原因记录为 JSON，前端 "执行轨迹" 面板可查看
- **三步/六步模式切换**：侧边栏开关，关闭时完全走原有流程，零侵入

---

## 7. 关键代码位置速查

| 要找什么 | 文件 | 行号 |
|---|---|---|
| LLM API 配置（DeepSeek） | `rag.py` | 48-60 |
| 中文→英文检索词 Prompt | `query_builder.py` | 47-83 |
| 答案生成 Prompt | `rag.py` | 385 |
| DeepSeek API 调用 | `rag.py` | 602 |
| BM25 索引构建 | `vector_db.py` | 130-206 |
| 混合检索 RRF 融合 | `vector_db.py` | 769-793 |
| ChromaDB 连接初始化 | `vector_db.py` | 110-116 |
| 多轮追问 UI 开关 | `app.py` | 264-289 |
| 检索模式 + 向量权重 UI | `app.py` | 第"检索方式"段 |
| 质检面板（前端） | `app.py` | 747-800 |
| 文字分块参数（1800 字符 + 200 重叠） | `vector_db.py` | `split_text()` 函数 |
| 评估框架入口 | `eval_runner.py` | `run_evaluation()` |
| 质检工作流 | `qa_pipeline.py` | `check_completeness()` / `check_safety()` / `check_evidence_relevance()` |
| Rerank 精排 | `reranker.py` | `rerank_documents()` |
| 三步Agent控制器 | `challenge_agent.py` | `run_3step_agent()` |
| 三层引用核验 | `challenge_agent.py` | `step2_verify()` |
| 证据门控拒答 | `evidence_gate.py` | `evaluate_evidence()` / `exceeds_clinical_boundary()` |
| 执行轨迹面板（前端） | `app.py` | 第 906-953 行 |
| 三步Agent开关 | `app.py` | 第 342-353 行 |
