# 拒答机制（Evidence Gate）

## 三层防线

```
检索完成 → 候选证据列表
  │
  ├─① 证据门控 (前置)
  │   evaluate_evidence() 规则判断
  │   ├─ 临床边界越界 → OUT_OF_CLINICAL_SCOPE
  │   ├─ 证据不足     → INSUFFICIENT_DIRECT_EVIDENCE
  │   └─ 缺少指定类型 → MISSING_REQUIRED_EVIDENCE_TYPE
  │   ↓ 命中任一 → build_structured_refusal() → 直接拒答
  │
  ├─② LLM 自助拒答 (Prompt 注入)
  │   evidence_safety_rules 写入 Prompt:
  │   "If fewer than 2 evidence items directly support
  │    the answer → output: [[EVIDENCE_GATE_REFUSE]]"
  │   ↓ 模型返回标记 → build_model_refusal() → 拒答
  │
  └─③ 空回答保护
      LLM 返回空字符串 → build_model_refusal() → 拒答
```

## 拒答场景详解

### ① 临床边界越界 `OUT_OF_CLINICAL_SCOPE`
```
匹配规则（正则）:
  "帮我/替我/直接给我" + "诊断/确诊/开药/开处方/停药/换药/调整剂量"
  "帮/替/给我" + "开" + "药"    ← 例："帮我开个降压药"
  "我" + "是不是/是否" + "得了/患有"
  "我应该/我能不能" + "停药/加量/减量/换药"
  "我该吃什么药/我应该吃什么药/给我推荐具体药物"
  "服用/吃" + "多少毫克/多大剂量/几片"
```

### ② 证据不足 `INSUFFICIENT_DIRECT_EVIDENCE`
```
条件: 检索到的证据数 < 1 且 没有指南/系统综述/Meta分析
说明: 即使有1条高质量证据(指南/系统综述/Meta分析)也放行
优化: min_reliable_evidence 已从2降为1，避免过度拒答
```

### ③ 缺少指定证据类型 `MISSING_REQUIRED_EVIDENCE_TYPE`
```
用户明确要求: "只用指南" → 检索结果中没有指南 → 拒答
用户宽松表达: "指南或研究" → 不触发此规则，正常回答
```

## 结构化拒答格式

```
## 当前判断
- 拒答原因 + 原因代码

## 已检索到的证据
- 证据数量、类型分布
- 前3条标题+编号

## 证据缺口
- 缺少哪些信息

## 建议下一步检索
- 具体的英文检索式建议

## 安全提示
- 本结果仅用于证据检索与研究辅助...
```

## 调用位置

| 环节 | 位置 | 说明 |
|------|------|------|
| 证据门控 | `rag.py:1109` answer_question() | Rerank 之后、LLM 调用之前 |
| 模型自助 | `rag.py:1162` Prompt 注入 | evidence_safety_rules 写入 System Prompt |
| 空回答兜底 | `rag.py:1181` 返回检查 | LLM 返回空字符串时触发 |
| 三步Agent | `challenge_agent.py:576` | step3 决策阶段复用 |
| 评估框架 | `eval_runner.py:227` | 检测拒答记录 |
