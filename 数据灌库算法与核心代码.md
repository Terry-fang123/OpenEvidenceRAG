# 数据灌库核心算法

## 1. 文本清洗 `clean_text()`

```
原始文本 (含PubMed XML标签)
  │
  ├─→ 正则去除 <tag> 标签    re.sub(r"<[^>]+>", " ", text)
  ├─→ HTML实体解码           html.unescape()  (&amp;→&, &lt;→<)
  ├─→ 合并连续空白            re.sub(r"\s+", " ", text)
  └─→ 去除首尾空格            .strip()
  │
  ▼
纯文本
```

## 2. 长文本切分 `split_text(text, max_chars=1800, overlap=200)`

```
输入: 纯文本
  │
  ├── 先调用 clean_text() 清洗
  ├── 长度 ≤ 1800 → 直接返回 [原文]
  │
  ├── 否则进入滑动窗口循环：
  │    │
  │    ├─ 窗口 [start, start+1800]
  │    │
  │    ├─ 在窗口后半段 [start+900, end] 内找断句点：
  │    │   · 英文句号 ". "    · 中文句号 "。"
  │    │   · 英文分号 "; "    · 中文分号 "；"
  │    │   → 取最靠后的位置 (max of rfind)
  │    │
  │    ├─ 切出 chunk = text[start : 断句点+1]
  │    │
  │    ├─ 下一窗口起点 = 断句点 - 200 (保留重叠)
  │    │
  │    ├─ 防死循环：下一窗口 ≤ 当前start → 强制推进到 end
  │    │
  │    └─ 循环直到 start ≥ 文本长度
  │
  ▼
输出: 文本块列表 [chunk1, chunk2, ...]
```

**图示**：

```
┌─────────────────── chunk1 ───────────────────┐
│  高血压是一种常见的慢性疾病。长期控制血压需要  │
│  坚持服药...                                 │
└──────────────────────────┬───────────────────┘
                    200字符重叠
┌──────────────────────────┴───────────────────┐
│            chunk2                             │
│  坚持服药，停药后血压常会回升至治疗前水平...    │
└───────────────────────────────────────────────┘
```

## 3. 向量编码 `model.encode()`

```
文本块列表 [chunk1, chunk2, ...]
  │
  ▼
SentenceTransformer('paraphrase-multilingual-MiniLM-L12-v2')
  │  normalize_embeddings=True  (余弦相似度 = 内积)
  │  batch_size=32              (批量推理)
  │
  ▼
384维 float32 向量矩阵 (N × 384)
```

## 4. 存储写入

| 目标库 | 格式 | 方式 |
|------|------|------|
| chroma_db | ChromaDB 专有 | `collection.upsert()` 自动去重 |
| evidence_store | numpy + JSONL | `.tmp`→`replace` 原子写入 |

## 5. 关键容错

| 问题 | 解法 |
|------|------|
| 断句点找不到 | 用 max_chars 硬截断 |
| 回溯导致死循环 | `next_start ≤ start` → 强制跳 `end` |
| 写入中断损坏旧文件 | 先写 `.tmp`，成功再 `replace` |
| 同一 PMID 重复导入 | upsert 自动覆盖 + 导入前去重 |
